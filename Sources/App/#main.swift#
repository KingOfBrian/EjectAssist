import Vapor

let drop = Droplet()

drop.get { req in
    return try drop.view.make("eject", [])
}

drop.post("eject") { req in
    let name = req.data["name"]
    let data = req.data["image"] // or req.multipart["image"]
}

drop.run()

	